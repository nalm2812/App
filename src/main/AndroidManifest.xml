<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.X9">
        <activity
            android:name=".DropdownMenu"
            android:exported="false"
            android:label="@string/title_activity_dropdown_menu"
            android:theme="@style/Theme.X9" />
        <activity
            android:name=".TrafficReportItem"
            android:exported="false"
            android:label="@string/title_activity_traffic_report_item"
            android:theme="@style/Theme.X9" />
        <activity
            android:name=".TrafficReportActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.X9">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>